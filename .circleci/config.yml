version: 2.1

jobs:
  build_push:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - setup_remote_docker:
          version: 20.10.7
      - run: ./build_and_push.sh

workflows:
  version: 2.1
  build_push:
    jobs:
      - build_push:
          context: shardlabs
          filters:
            branches:
              only:
                - master
